export default class Scheduler {
    constructor({techees, dateRange}) {
        this.counter = {};
        this.techees = {};
        this.counter = {};
    }

    resetCounter() {
        this.counter = {}; // use names
    }
}